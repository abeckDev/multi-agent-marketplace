# API base URL (relative path for production, full URL for cross-origin dev)
VITE_API_BASE_URL=/api

# Optional: Override the Vite proxy target (defaults to http://localhost:8000)
# For unified server (recommended): http://localhost:8000
# For legacy UI server: http://localhost:5000
# VITE_API_TARGET=http://localhost:8000